import React, { Component } from "react";

class Button extends Component {
  render() {
    return (
      <button type="button">
        <span>Check Out</span>
      </button>
    );
  }
}

export default Button;
